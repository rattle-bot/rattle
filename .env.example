# === TELEGRAM CONFIGURATION ===
# Telegram Bot Token (get from BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here
# Comma-separated list of Telegram chat IDs to send messages to
# Example: 123456789,-987654321
TELEGRAM_CHAT_IDS=

# === ENVIRONMENT ===
# Application environment: local, dev, prod, etc.
APP_ENV=local
# Log level: debug, info, warn, error
LOG_LEVEL=debug

# === ERROR FILTERING CONFIG ===
# Comma-separated regex patterns (case-insensitive) to INCLUDE as errors
# Example: (?i)(^|\s|:|]|\[)ошибка(:|\s|$)
INCLUDE_ERROR_PATTERNS=(?i)(^|\s|:|]|\[)ошибка(:|\s|$)
# Comma-separated regex patterns to EXCLUDE (even if they match include)
# Example: (?i)TelegramNetworkError.*Request timeout
EXCLUDE_ERROR_PATTERNS=(?i)TelegramNetworkError.*Request timeout

# === CONTAINER FILTERING ===
# Comma-separated list of container names to ignore (e.g. redis, nginx)
EXCLUDE_CONTAINER_NAMES=
# Comma-separated list of container images to ignore
EXCLUDE_CONTAINER_IMAGES=
# Comma-separated list of container IDs to ignore (can be partial prefixes)
EXCLUDE_CONTAINER_IDS=